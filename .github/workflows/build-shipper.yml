name: Build YB Shipper Image

on:
  workflow_dispatch:
    inputs:
      yb_version:
        description: "YugabyteDB version (e.g. 2025.2.2.2)"
        required: true
        default: "2025.2.3.2"
      yb_build:
        description: "Build number (e.g. b11)"
        required: true
        default: "b1"
      yb_sha256:
        description: "Expected SHA256 for the YugabyteDB tarball"
        required: true
        default: "ea23027f6cc46f0b87123e712f6a0cdbb9676c84d6bc8f4f0124b104de556f97"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/yb-shipper

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: shipper
          push: true
          build-args: |
            YB_VERSION=${{ inputs.yb_version }}
            YB_BUILD=${{ inputs.yb_build }}
            YB_SHA256=${{ inputs.yb_sha256 }}
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ inputs.yb_version }}-${{ inputs.yb_build }}
